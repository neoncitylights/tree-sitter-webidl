============================================
Namespace statement with operations
============================================

namespace CSS {
    CSSUnitValue number(double value);
    CSSUnitValue percent(double value);
};

----

(source_file
	(namespace_statement
		(identifier)
		(namespace_member
			(regular_operation
				(type
					(single_type
						(distinguishable_type
							(identifier))))
				(operation_rest
					(operation_name
						(identifier))
					(argument_list
						(argument
							(type
								(single_type
									(distinguishable_type
										(identifier))))
							(argument_name
								(identifier)))))))
		(namespace_member
			(regular_operation
				(type
					(single_type
						(distinguishable_type
							(identifier))))
				(operation_rest
					(operation_name
						(identifier))
					(argument_list
						(argument
							(type
								(single_type
									(distinguishable_type
										(identifier))))
							(argument_name
								(identifier)))))))))
